servers:
  - url: "wss://your-server.com"
    name: "主节点"

token: "CHANGE-ME-same-as-server-config"

# shell: /bin/zsh
auto_connect: true
prevent_sleep: true
reconnect_max_interval: 30
local_buffer_size: 131072
max_sessions: 10
